{% extends 'base.html.twig' %}

{% block title %} 
 {% if edit is defined %}
        Editar tarea
    {% else %}
        Crear tarea
    {% endif %}
    {% endblock %}

{% block body %}

<div class="example-wrapper">
    <h2>

       {% if edit is defined %}
                Editar tarea
            {% else %}
                Nueva Tarea
            {% endif %}✅</h2>
     {{ form_start(tarea_form) }}
    {{ form_widget(tarea_form) }}
    {{ form_end(tarea_form) }}
   
</div>
{% endblock %}